name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: rshc-linux-x64
            runner_artifact: rshc-runner-linux-x64
          - target: x86_64-apple-darwin
            os: macos-latest
            artifact: rshc-macos-x64
            runner_artifact: rshc-runner-macos-x64
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: rshc-macos-arm64
            runner_artifact: rshc-runner-macos-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binaries
        run: |
          cp target/${{ matrix.target }}/release/rshc ${{ matrix.artifact }}
          cp target/${{ matrix.target }}/release/rshc-runner ${{ matrix.runner_artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}
            ${{ matrix.runner_artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare binaries
        run: |
          mkdir -p binaries
          cp artifacts/rshc-linux-x64/rshc-linux-x64 binaries/
          cp artifacts/rshc-linux-x64/rshc-runner-linux-x64 binaries/
          cp artifacts/rshc-macos-x64/rshc-macos-x64 binaries/
          cp artifacts/rshc-macos-x64/rshc-runner-macos-x64 binaries/
          cp artifacts/rshc-macos-arm64/rshc-macos-arm64 binaries/
          cp artifacts/rshc-macos-arm64/rshc-runner-macos-arm64 binaries/
          chmod +x binaries/*

      - name: Release
        uses: cycjimmy/semantic-release-action@v6
        with:
          extra_plugins: |
            @semantic-release/exec
            @semantic-release/git
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get release tag
        id: get_tag
        run: |
          TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Upload binaries to release
        if: steps.get_tag.outputs.tag != ''
        run: |
          TAG="${{ steps.get_tag.outputs.tag }}"
          gh release upload "$TAG" binaries/rshc-linux-x64 binaries/rshc-runner-linux-x64 binaries/rshc-macos-x64 binaries/rshc-runner-macos-x64 binaries/rshc-macos-arm64 binaries/rshc-runner-macos-arm64 --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
